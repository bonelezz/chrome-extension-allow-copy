{
  "name": "allow-copy",
  "version": "0.0.1",
  "description": "allow copy on some websites",
  "main": "src/js/index.js",
  "scripts": {
    "clean": "rm -rf dist allow-copy.zip",
    "chrome": "cp src/chrome/* dist/",
    "build": "rollup -c ",
    "zip": "zip -r allow-copy.zip dist",
    "publish": "npm run clean; npm run build && npm run chrome && npm run zip",
    "dev": "DEV=true chokidar 'src/**' -c 'npm run build' -t 500 --initial",
    "test": "echo 'nothing to test'"
  },
  "author": "FallenMax@gmail.com",
  "license": "ISC",
  "devDependencies": {
    "chokidar-cli": "1.2.0",
    "rollup": "0.34.10",
    "rollup-plugin-replace": "1.1.1",
    "rollup-plugin-string": "^2.0.2"
  }
}
